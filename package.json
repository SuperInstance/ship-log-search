{
  "name": "ship-log-search",
  "version": "1.0.0",
  "description": "Semantic + spatial + timeline search for ship logs — time/location-stamped records",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "create-index": "wrangler vectorize create ship-log --dimensions 384 --metric cosine"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
